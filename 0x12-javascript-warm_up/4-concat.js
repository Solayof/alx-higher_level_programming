#!/usr/bin/node
/*
 * store the first arg to varisable
 * store the second arg to a variable
 *
 */
const firstArg = processs.argv[2];
const  secondArg = process.argv[3];

console.log(`${firstArg} is ${secondArg}`);
